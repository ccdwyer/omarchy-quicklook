You are a Fable 5 fix agent for the Omarchy plugin competition. Work ONLY inside the plugin directory given below. Grok wrote the initial implementation; GPT-5.6 Sol reviewed it and it FAILED. Your job: apply the fixes to pass the gate.

PLUGIN DIR: /Users/chris/Developer/OmarchyPlugins/quicklook
SPEC (authoritative; its "Tribunal review" applied-changes section supersedes earlier contradictions): /Users/chris/Developer/OmarchyPlugins/docs/04-quicklook.md
PLATFORM REFERENCE (authoritative for manifest/IPC/settings): /Users/chris/Developer/OmarchyPlugins/docs/quattro-shell-reference.md

CONSTRAINTS:
- macOS build host: you CANNOT run Hyprland/Quickshell/qmllint/cargo-on-target. Write for correctness by inspection; keep QML conservative (Quickshell.Io Process/Socket/FileView, Quickshell.Hyprland, standard QtQuick). Do NOT invent Quickshell APIs; prefer documented IPC (omarchy-shell shell call/summon/hide/toggle) over undocumented host shortcuts (serviceFor etc.).
- Linux prebuilt binaries cannot be produced here — use a CI release workflow + honest source-build docs; never commit fake binaries.
- Do NOT delete review/log dotfiles; keep them gitignored.
- Every fix must be real and complete (no TODO/stubs in core paths). Add/repair regression tests for each fixed blocker where a test harness exists (node for JS engines, cargo tests for Rust, .sh for shell).

TASK: Read the GPT review below (the latest .gpt_review_r*.md), fix EVERY blocker and address the warnings. Re-run whatever tests run off-device and confirm green. Update ASSUMPTIONS.md. Then `git commit` with a message like "fix: address review round N (fable)".

Your final message: a terse list of what you changed per blocker, and any blocker you believe is a false positive (with reasoning).

REVIEW TO ADDRESS:
GATE: FAIL
BLOCKERS:
1. Service.qml:278-326,366-406,532-556; Overlay.qml:245-337 — Quattro `shell call <id> <method> <arg>` invokes methods on the loaded entry point, but `snapshot` and `theme` exist only on a separate `IpcHandler`, and root `preview(path,page)` interprets the overlay’s JSON argument as a literal path. Expose root-level string adapters for every shell-call method, including `snapshot(arg)`, `theme(json)`, and JSON-parsing `preview(arg)`.
2. compat/quicklookd.py:409-453 — Captured subprocess output uses unbounded `PIPE` plus `communicate()`, allowing noisy `find`, Poppler, or image tools to exhaust memory. Concurrently drain each pipe, retain at most 96 KiB, discard excess until EOF, and add oversized-output regression coverage.
3. compat/quicklookd.sh:379-446,555-603 — `find`/`locate` can write unlimited temporary output, code reads 200 KiB, and a single CSV row can exceed every intended bound. Cap all path-consuming subprocess reads and temporary output at 96 KiB inside the watchdog path, including oversized no-newline inputs.
4. compat/quicklookd.py:421-427; tests/killable.test.sh:71-77 — Production’s `limits=True` branch suppresses `setsid()` failure and proceeds with a nonexistent process-group guarantee, while the behavioral test exercises only `limits=False`. Make session creation failure abort/disable path-consuming features and test TERM-ignoring descendants through the production branch.
5. src/quicklookd/src/limits.rs:61-80,83-110 — Contrary to the stated bounded-primary-path assumption, `run_limited` neither creates/kills a subprocess group nor bounds/drains piped output; a full pipe can deadlock until timeout, and descendants survive `child.kill()`. Establish a new process group, drain with a 96 KiB retention cap, and perform group TERM-then-KILL with behavioral descendant/output tests.
WARNINGS:
1. tests/killable.test.sh:30-49 — The shell regression generally exercises GNU `timeout` when installed, leaving the portable `setsid` implementation untested.
2. README.md:134-138 — The direct typed `IpcHandler` example omits its required string argument and conflates direct target IPC with Quattro shell-call routing.
SUMMARY: The manifest kinds, entry points, inline settings, `keepLoaded`, keybinds, renderer scope, and honesty constraints otherwise conform; no `barWidget` block is applicable because this is not a bar-widget plugin. Round 10 fails because the fallback bounds and watchdog proof remain incomplete, shell-call routing is broken on the authoritative host contract, and the Rust subprocess implementation is not actually group-safe or output-bounded.
