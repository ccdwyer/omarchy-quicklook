# Harbor Notes

A tiny sample document shipped with QuickLook so the overlay is useful before your home directory has been indexed.

- Invoice #1042 lives next to this file
- `sales.csv` has five thousand rows of pretend retail data
- `themed.rs` shows syntax colors from the active Omarchy palette
- `photo.png` is a generated still, not a network fetch

Everything is local. Nothing leaves the machine.
