GATE: FAIL
BLOCKERS:
1. `src/quicklookd/src/limits.rs:127-143; compat/quicklookd.py:499-507` — descendant cleanup only reaches KILL on timeout. If the group leader exits successfully while a TERM-ignoring descendant retains stdout/stderr, Rust sends TERM and then blocks forever joining drain threads; Python returns after two-second joins while the descendant and drain threads survive. This contradicts the claimed unconditional descendant kill and can hang/leak on hostile renderer input. On every leader exit, TERM the process group, wait briefly, then unconditionally KILL the group before joining drains; add a test where the leader exits immediately after spawning a TERM-ignoring child that inherits both pipes.
WARNINGS:
1. `ASSUMPTIONS.md:52-53; src/quicklookd/src/limits.rs:177-204` — documentation says `RLIMIT_AS` is Linux-only, but the implementation applies it on every Unix target. Align the cfg or the claim.
2. `compat/quicklookd.sh:107-122` — the GNU `timeout` branch lacks the portable branch’s explicit unconditional group KILL after a normally exiting leader. Apply the same post-exit cleanup guarantee.
SUMMARY: The Round 10 output caps, process-group creation, timeout TERM/KILL behavior, root-level IPC adapters, and removal of `RLIMIT_NPROC` are present. The remaining early-leader-exit hole means renderer descendants can still hang the Rust helper or survive in compatibility paths, so the submission is not yet judge-safe.
