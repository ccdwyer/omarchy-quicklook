GATE: FAIL
BLOCKERS:
1. compat/quicklookd.sh:107-124 — GNU-timeout fallback without `setsid` runs in the foreground and loses the process-group ID, so an early-exiting leader can leave a TERM-ignoring descendant alive holding pipes. Launch GNU `timeout` in the background, record its PGID, wait for it, then perform group TERM → grace → unconditional KILL before returning; otherwise disable the path when group isolation cannot be guaranteed.
2. compat/quicklookd.sh:684-692 — `run_isolated` still sets `ulimit -u 32` (`RLIMIT_NPROC`), contradicting ASSUMPTIONS.md and potentially preventing renderers from starting for a judge already running more than 32 processes. Remove the process-count limit; retain CPU, file-size, Linux-only address-space, and process-group wall-clock enforcement.
WARNINGS:
1. compat/quicklookd.sh:79-121 — normal leader-exit cleanup performs immediate KILL without the documented TERM-and-grace phase. This prevents leakage where group signaling is available, but does not match the stated uniform teardown contract.
SUMMARY: Rust and Python now kill surviving process groups before joining output drains, covering both timeout and normal leader exit. The POSIX fallback still has an unprotected GNU-timeout branch and an unsafe process-count limit, either of which can fail on a judge’s machine.
