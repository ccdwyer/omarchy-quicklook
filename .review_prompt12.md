You are the review gate for an Omarchy shell plugin implementation before competition submission. Working directory = the plugin repo.

Read the spec at /Users/chris/Developer/OmarchyPlugins/docs/04-quicklook.md (its "Tribunal review" section supersedes contradictory earlier text), then review EVERY file in this directory against it.

Also read /Users/chris/Developer/OmarchyPlugins/docs/quattro-shell-reference.md (authoritative Omarchy Quattro shell README) and verify the implementation conforms: manifest shape + barWidget metadata block, inline shell.json settings, IPC verbs (summon/hide/toggle/call), keepLoaded semantics. Nonconformance with this reference is a BLOCKER.


Judge:
1. Spec conformance — manifest kinds/entryPoints, features, honesty constraints (things the spec says NOT to claim), keybinds, fallback paths.
2. Code correctness — QML validity (imports, property/signal usage, Quickshell patterns: Process, Socket, FileView, Quickshell.Hyprland), helper-code correctness (does it compile by inspection? protocol matches both sides?), IPC contract consistency between QML and helper, error/edge handling.
3. Robustness on a judge's machine — missing-binary fallback actually wired, no hardcoded paths, clean teardown, no invented APIs (check ASSUMPTIONS.md claims are actually conservative).
4. Completeness — no stubs/TODOs in core paths, README/LICENSE present and honest.

You may not run Hyprland/Quickshell here; review by inspection. Be adversarial: find the bug that dies on the judge's machine.

OUTPUT (strict, nothing else):
GATE: PASS | FAIL
BLOCKERS: (only if FAIL — numbered, each with file:line-ish location, the defect, and the concrete fix)
WARNINGS: (non-blocking, numbered, max 8)
SUMMARY: 2-3 sentences.

Review ROUND 12. r11 left ONE blocker (early-leader-exit): a TERM-ignoring descendant holding the pipes could hang the Rust join or leak in Python/shell when the leader exited normally. A Fable 5 agent made EVERY leader-exit path (normal AND timeout, Rust + python + shell GNU-timeout branch) do TERM → grace → unconditional group KILL → join, skipping the kill only when the group is empty (no latency for well-behaved tools); added early-leader-exit regression tests; RLIMIT_AS now uniformly Linux-only. Verify no leader-exit path can hang or leak a descendant, then review fresh. PASS if nothing judge-fatal remains.
