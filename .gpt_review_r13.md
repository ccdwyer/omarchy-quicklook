GATE: PASS
WARNINGS:
1. ASSUMPTIONS.md:38 — stale claim says the POSIX PDF path requires `timeout`; implementation also safely supports the manual `setsid`/`os.setsid` watchdog. Update the wording.
2. compat/quicklookd.sh:814-825 — pure-shell PDF fallback always reports `page_count: 1`, so multi-page navigation is unavailable when both Rust and Python helpers are absent.
3. compat/quicklookd.sh:620-665 — pure-shell CSV parsing does not honor quoted delimiters or multiline fields; document this degraded-mode limitation.
4. Repository root — historical review prompts/logs are tracked. They are export-ignored and nonfunctional, but should be removed before submission for a cleaner artifact.
SUMMARY: Round 12’s shell-fallback blockers are fixed: GNU timeout is group-isolated or disabled, NPROC is uncapped, and both watchdog paths perform TERM, grace, then unconditional group KILL whenever survivors remain. Inspection found no judge-fatal spec, Quattro manifest/settings/IPC/keepLoaded, QML, helper-protocol, fallback, or teardown defect.
