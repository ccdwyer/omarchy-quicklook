# No prebuilt musl binaries are committed from the macOS authoring host.
# Authentic x86_64 / aarch64 helpers are produced only by
#   .github/workflows/release.yml
# which publishes sha256 lines for:
#   quicklookd-x86_64-unknown-linux-musl
#   quicklookd-aarch64-unknown-linux-musl
# scripts/fetch-helper.sh will not install a download that is not in that list.
# Until a release exists, `omarchy plugin add --enable` uses compat/ + samples/.
